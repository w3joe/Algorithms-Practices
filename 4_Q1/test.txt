#include <iostream>
#include <vector>
#include<string>
using namespace std;
int main()
{
    string textinput;
    int N = 0;
    getline(cin, textinput);
    for (int i = 0; i < textinput.length(); i++)if (isspace(textinput[i]))N++ ;
    cout << N;
}